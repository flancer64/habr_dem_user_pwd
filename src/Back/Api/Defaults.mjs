/**
 * Plugin constants (hardcoded configuration) for backend code.
 */
export default class Fl64_Dem_User_Pwd_Back_Api_Defaults {

    NAME = '@flancer64/habr_dem_user_pwd';

    constructor() {
        Object.freeze(this);
    }
}
